---
/**
 * Thin shim re-export. The real implementation lives in the shared deck-shell
 * at @dididecks/shell/components/PageAsDeckWrapper.astro (promoted from
 * chroma-decks on 2026-06-06; see
 * dididecks-ai/context-v/plans/Lift-Chroma-Decks-Generic-Code-into-Shared-Shell.md).
 *
 * reach-edu-hub adopted the shell on 2026-06-29 (see
 * context-v/plans/Integrate-Reach-Edu-Hub-into-Dididecks-Shell.md). Keeping this
 * file as a re-export means the scroll-deck pages keep importing the stable local
 * path while getting the shared implementation (scroll-snap, keyboard nav,
 * #s-N hash, section counter, chrome/fullscreen toggles, reveal-item observer).
 *
 * Public API unchanged from the original local port:
 *   - enableScrollSnap?: boolean       (default true)
 *   - showNavigationHints?: boolean    (default true)
 *   - hintDuration?: number            (default 2400)
 */
import ShellPageAsDeckWrapper from "@dididecks/shell/components/PageAsDeckWrapper.astro";
---
<ShellPageAsDeckWrapper {...Astro.props}>
  <slot />
</ShellPageAsDeckWrapper>
